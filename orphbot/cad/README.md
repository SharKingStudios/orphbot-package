# OrphBot CAD Sources

Put source CAD files here, for example `orphbot_body.step`.

For RViz, export or convert the body to `../meshes/orphbot_body.stl`. The URDF launch path detects that STL automatically after `colcon build --symlink-install`.
